<%default_image = "https://i.imgur.com/WO2Uui9.jpg"%>
<div class=" products-list">
<h1>Categories:</h1>

  <section class="row all-categories">
      <% @categories.each do |category| %>
          <div class="category-card">
            <%if category.products.first%>
              <%= link_to  category_path(category.id) do %> 
                  <%= image_tag category.products.first.image, alt: "product's image", class:'category-image'%>
                  <span><%= category.name %></span>
              <% end %>
            <%else%>
               <%= link_to category_path(category.id) do %> 
                  <%= image_tag default_image, alt: "default product's image", class:'category-image'%>
                  <span><%= link_to category.name %></span>
                <% end %>
            <%end%>
          </div>
      <% end %> 
  </section>

  <%= link_to "Add a new category", new_category_path, class: "btn btn-primary" %>
</div>